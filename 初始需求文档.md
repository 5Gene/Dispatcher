# Action Dispatch - 初始需求文档

## 项目概述

开发一个通用的、高性能的、基于属性宏的 Action 注册与分发系统，用于 Rust 语言。

## 核心需求

### 1. 声明式注册机制

#### 1.1 使用属性宏标记处理函数
- 提供 `#[action(...)]` 属性宏
- 允许开发者通过注解标记处理函数，自动完成注册
- 无需手动调用注册函数

#### 1.2 支持的注解参数
| 参数 | 类型 | 是否必需 | 默认值 | 说明 |
|------|------|---------|--------|------|
| `regex` | `&str` | ✅ 必需 | - | 用于匹配 key 的正则表达式 |
| `priority` | `i32` | ❌ 可选 | `0` | 优先级，数值越大优先级越高 |
| `description` | `&str` | ❌ 可选 | `""` | 描述信息，用于调试和文档 |
| `sync` | `bool` | ❌ 可选 | `false` | 是否启用全局同步执行模式 |
| `by_ref` | `bool` | ❌ 可选 | `false` | 是否使用引用传递（零拷贝） |

#### 1.3 函数签名约束
- 必须是自由函数（不在 `impl` 块中）
- 必须恰好有一个参数
- 支持两种参数形式：
  - `fn(T)` - 值传递（会拷贝事件）
  - `fn(&T)` - 引用传递（零拷贝，需设置 `by_ref = true`）
- 所有 action 必须使用相同的事件类型 `T`
- 返回值不限（可以是任意类型或 `()`）

#### 1.4 示例
```rust
#[action(regex = r"user/\d+/read", priority = 5, sync = false)]
fn handle_user_read(event: MyEvent) {
    println!("读取用户");
}

#[action(regex = r"user/\d+/update", priority = 10, sync = true)]
fn handle_user_update(event: MyEvent) {
    println!("更新用户");
}

#[action(regex = r"large/.*", priority = 3, by_ref = true)]
fn handle_large_event(event: &MyEvent) {
    println!("处理大事件（零拷贝）");
}
```

---

### 2. 事件分发机制

#### 2.1 分发函数签名
```rust
pub fn dispatch<T>(key: &str, event: T) -> Result<(), DispatchError>
where
    T: 'static + Send + Sync
```

#### 2.2 执行流程
1. 获取全局锁（阻塞直到获取成功）
2. 在锁保护下进行匹配：
   - 遍历所有已注册的 action
   - 使用正则表达式匹配 `key`
   - 找到优先级最高的匹配项
3. 若无匹配，释放锁并返回 `Err(DispatchError::NoMatch)`
4. 若匹配成功，根据 `sync` 标志决定执行策略：
   - **`sync = false`（默认）**：
     - 立即释放锁
     - 执行处理函数
     - 支持多个 action 并发执行
   - **`sync = true`**：
     - 保持持有锁
     - 执行处理函数
     - 执行完成后释放锁
     - 期间阻塞所有其他 `dispatch` 调用

#### 2.3 示例
```rust
// 分发事件
dispatch("user/123/read", MyEvent {
    user_id: 123,
    action: "read".to_string(),
}).unwrap();

// 处理错误
match dispatch("unknown/key", event) {
    Ok(()) => println!("分发成功"),
    Err(DispatchError::NoMatch) => println!("没有匹配的 action"),
    Err(DispatchError::Poisoned) => println!("锁已被污染"),
}
```

---

### 3. 全局同步锁机制

#### 3.1 核心概念
系统维护一个全局互斥锁，所有 `dispatch` 调用都会竞争此锁。

#### 3.2 两种执行模式

| 模式 | 行为 | 适用场景 | 并发性 |
|------|------|----------|--------|
| `sync = false`（默认） | 拿锁 → 匹配 → **释放锁** → 执行 | 普通操作（读取、查询等） | ✅ 支持并发 |
| `sync = true` | 拿锁 → 匹配 → **持有锁** → 执行 → 释放锁 | 关键操作（更新、迁移、配置变更等） | ❌ 全局排他 |

#### 3.3 设计思想
- 所有 `dispatch` 请求入口串行化（竞争同一把锁）
- `sync = false` 的 action 快速释放锁 → 允许并发执行
- `sync = true` 的 action 持有锁直到完成 → 阻塞所有其他操作
- 适用于需要原子性保证的关键操作

#### 3.4 使用场景示例

**适合 `sync = false` 的场景：**
- 读取用户信息
- 查询数据
- 发送通知
- 日志记录

**适合 `sync = true` 的场景：**
- 数据库迁移
- 配置文件更新
- 全局状态变更
- 系统初始化/关闭

---

### 4. 注册与缓存机制

#### 4.1 编译期注册
- 使用过程宏（procedural macro）实现
- 利用 `inventory` crate 在编译期/启动期自动收集所有 handler
- 无需手动注册

#### 4.2 全局注册表
- 全局静态变量存储所有 action handler
- 包含以下信息：
  - 编译后的正则表达式（`Regex` 对象）
  - 优先级（`priority`）
  - 描述信息（`description`）
  - 同步标志（`sync`）
  - 引用传递标志（`by_ref`）
  - 类型擦除的函数指针（`fn(*const ())`）
- 按优先级降序排序（高优先级在前）
- 初始化后不可变（只读）

#### 4.3 性能优化（分层匹配）
为了提升匹配性能，注册表采用分层结构：

| 层级 | 匹配类型 | 时间复杂度 | 示例正则 |
|------|---------|-----------|---------|
| 第一层 | 精确匹配 | O(1) | `^user/123$` |
| 第二层 | 前缀匹配 | O(m) | `^user/.*` |
| 第三层 | 复杂正则 | O(k) | `^user/\d+/.*` |

**查找流程：**
1. 先尝试精确匹配（HashMap 查找，O(1)）
2. 再尝试前缀匹配（按前缀长度降序遍历，O(m)）
3. 最后尝试复杂正则（按优先级遍历，O(k)）

---

### 5. 类型安全与线程安全

#### 5.1 类型安全
- 使用泛型参数 `T` 保证类型一致性
- 编译期类型检查，避免运行时类型错误
- 使用类型擦除技术（type erasure）统一不同函数签名
- 无需序列化/反序列化

#### 5.2 线程安全
- 完全线程安全，支持多线程并发调用 `dispatch`
- 使用 `std::sync::RwLock` 实现全局同步
  - 读锁：多个 `sync = false` 的 action 可并发执行
  - 写锁：`sync = true` 的 action 独占执行
- 事件类型必须满足 `Send + Sync` 约束
- 编译期自动检查线程安全性

#### 5.3 类型擦除技术
```rust
// 用户定义的函数
fn handle_user(event: MyEvent) { ... }

// 宏生成的包装函数（类型擦除）
fn wrapper(ptr: *const ()) {
    unsafe {
        let event = std::ptr::read(ptr as *const MyEvent);
        handle_user(event);
    }
}

// 注册时存储为 fn(*const ())
// 执行时通过原始指针还原类型
```

---

### 6. 错误处理

#### 6.1 错误类型
```rust
#[derive(Debug, Clone, PartialEq, Eq)]
pub enum DispatchError {
    /// 没有找到匹配的 action
    NoMatch,
    
    /// 锁被 panic 污染（某个线程在持有锁时 panic）
    Poisoned,
}
```

#### 6.2 错误处理示例
```rust
match dispatch("some/key", event) {
    Ok(()) => {
        // 分发成功
    }
    Err(DispatchError::NoMatch) => {
        // 没有匹配的 action，可能是 key 错误或未注册
    }
    Err(DispatchError::Poisoned) => {
        // 锁被污染，某个线程 panic 了
        // 通常需要重启程序
    }
}
```

---

### 7. 调试与监控

#### 7.1 列出所有已注册的 action
```rust
pub fn list_actions() -> Vec<ActionInfo>

pub struct ActionInfo {
    pub regex: String,
    pub priority: i32,
    pub description: String,
    pub sync: bool,
    pub by_ref: bool,
}
```

#### 7.2 使用示例
```rust
// 列出所有已注册的 action
for action in list_actions() {
    println!(
        "regex: {}, priority: {}, sync: {}, by_ref: {}, description: {}",
        action.regex, action.priority, action.sync, action.by_ref, action.description
    );
}
```

---

### 8. 性能要求

#### 8.1 编译期性能
- ✅ 宏展开在编译期完成，零运行时开销
- ✅ `inventory` 收集在编译/链接期完成
- ✅ 无额外运行时代码生成

#### 8.2 启动期性能
- ✅ 首次调用 `dispatch` 时初始化注册表（延迟初始化）
- ✅ 正则表达式只编译一次并缓存
- ✅ 预先排序和构建分层索引

#### 8.3 运行时性能目标
| 操作 | 目标耗时 | 说明 |
|------|---------|------|
| 精确匹配 + 分发 | < 1 μs | HashMap 查找 + 函数调用 |
| 前缀匹配 + 分发 | < 5 μs | 少量前缀遍历 + 函数调用 |
| 复杂正则匹配 + 分发 | < 50 μs | 正则匹配 + 函数调用 |
| 获取全局锁（无竞争） | < 50 ns | RwLock 快速路径 |
| 锁竞争等待 | 取决于持有锁的 action 执行时间 | - |

#### 8.4 内存占用
- 每个 action 约 120-200 字节（不包括正则对象）
- 每个编译后的正则约 1-5 KB
- 对于 100 个 action，总内存占用约 200-500 KB

#### 8.5 性能优化措施
- ✅ 使用 RwLock 替代 Mutex（支持读并发）
- ✅ 分层匹配算法（精确 → 前缀 → 正则）
- ✅ 支持引用传递（`by_ref`），避免大事件拷贝
- ✅ 编译期注册，运行时无动态分配
- ✅ 预编译正则表达式

---

### 9. 可扩展性

#### 9.1 已实现
- ✅ 调试接口（`list_actions`）
- ✅ 引用传递支持（`by_ref`）
- ✅ 优先级匹配
- ✅ 全局同步模式

#### 9.2 未来可扩展功能
- ⏳ 超时机制（`timeout`）
- ⏳ 异步版本（`async fn` 支持）
- ⏳ 性能监控（`dispatch_with_stats`）
- ⏳ 条件分发（`dispatch_if`）
- ⏳ 批量分发（`dispatch_batch`）

---

### 10. 注意事项与最佳实践

#### 10.1 避免死锁
**❌ 错误示例（会导致死锁）：**
```rust
#[action(regex = r"task/.*", sync = true)]
fn bad_handler(event: MyEvent) {
    // ❌ 在 sync = true 的 action 中再次调用 dispatch 会死锁
    dispatch("another/task", event).unwrap();
}
```

**✅ 正确做法：**
1. 使用 `sync = false`（如果不需要全局排他）
2. 重构逻辑，避免嵌套调用
3. 使用异步版本（未来支持）

#### 10.2 性能考虑
- ⚠️ 谨慎使用 `sync = true`，会阻塞所有其他 `dispatch`
- ⚠️ 复杂的正则表达式会影响匹配速度
- ⚠️ 大事件应使用 `by_ref = true` 避免拷贝
- ⚠️ action 数量过多会增加匹配开销

#### 10.3 类型一致性
所有 action 必须使用相同的事件类型：

**❌ 错误示例：**
```rust
#[action(regex = r"type1/.*")]
fn handler1(event: EventA) { }  // 类型 A

#[action(regex = r"type2/.*")]
fn handler2(event: EventB) { }  // 类型 B - 编译错误！
```

**✅ 正确做法：**
```rust
enum Event {
    TypeA(EventA),
    TypeB(EventB),
}

#[action(regex = r"type1/.*")]
fn handler1(event: Event) {
    if let Event::TypeA(e) = event { ... }
}

#[action(regex = r"type2/.*")]
fn handler2(event: Event) {
    if let Event::TypeB(e) = event { ... }
}
```

---

## 技术栈

### 核心依赖
- **regex** (1.10+): 正则表达式引擎
- **once_cell** (1.19+): 全局静态变量延迟初始化
- **inventory** (0.3+): 编译期收集元数据

### 宏相关依赖
- **syn** (2.0+): Rust 语法解析
- **quote** (1.0+): 代码生成
- **proc-macro2** (1.0+): 过程宏基础设施

---

## 项目结构

```
action_dispatch/
├── Cargo.toml                    # Workspace 配置
├── README.md                     # 项目文档
├── 初始需求文档.md                # 本文档
├── action_dispatch/              # 主 crate（Re-export）
│   ├── Cargo.toml
│   ├── src/
│   │   └── lib.rs               # Re-export 所有功能
│   ├── examples/
│   │   ├── basic.rs             # 基础示例
│   │   └── concurrent.rs        # 并发示例
│   └── tests/
│       └── integration_test.rs  # 集成测试
├── action_dispatch_core/         # 核心运行时
│   ├── Cargo.toml
│   └── src/
│       └── lib.rs               # 注册表、锁、dispatch 函数
└── action_dispatch_macro/        # 属性宏
    ├── Cargo.toml
    └── src/
        └── lib.rs               # #[action] 宏实现
```

---

## 使用示例

### 基础示例
```rust
use action_dispatch::{action, dispatch};

#[derive(Clone)]
struct MyEvent {
    user_id: u64,
    action: String,
}

// 普通 action
#[action(regex = r"user/\d+/read", priority = 5)]
fn handle_read(event: MyEvent) {
    println!("读取用户: {}", event.user_id);
}

// 关键 action
#[action(regex = r"user/\d+/update", priority = 10, sync = true)]
fn handle_update(event: MyEvent) {
    println!("更新用户: {}（阻塞所有其他操作）", event.user_id);
}

// 大事件处理（引用传递）
#[action(regex = r"large/.*", priority = 3, by_ref = true)]
fn handle_large(event: &MyEvent) {
    println!("处理大事件（零拷贝）");
}

fn main() {
    dispatch("user/123/read", MyEvent {
        user_id: 123,
        action: "read".to_string(),
    }).unwrap();

    dispatch("user/456/update", MyEvent {
        user_id: 456,
        action: "update".to_string(),
    }).unwrap();
}
```

### 并发示例
```rust
use action_dispatch::{action, dispatch};
use std::thread;

#[derive(Clone)]
struct Event { id: u64 }

#[action(regex = r"normal/.*", sync = false)]
fn handle_normal(event: Event) {
    println!("并发执行: {}", event.id);
}

#[action(regex = r"critical/.*", sync = true)]
fn handle_critical(event: Event) {
    println!("独占执行: {}", event.id);
}

fn main() {
    let handles: Vec<_> = (0..10).map(|i| {
        thread::spawn(move || {
            dispatch("normal/task", Event { id: i }).unwrap();
        })
    }).collect();

    for h in handles {
        h.join().unwrap();
    }
}
```

---

## 测试要求

### 单元测试
- ✅ 基础分发功能
- ✅ 优先级匹配
- ✅ 正则表达式匹配
- ✅ 错误处理（NoMatch、Poisoned）

### 集成测试
- ✅ 多线程并发安全性
- ✅ 全局同步锁行为（sync = true/false）
- ✅ 引用传递（by_ref）

### 示例程序
- ✅ 基础示例（basic.rs）
- ✅ 并发示例（concurrent.rs）

---

## 许可证

MIT OR Apache-2.0（双许可证）

---

## 总结

本项目旨在提供一个：
- ✅ **易用的** - 声明式注解，无需手动注册
- ✅ **高性能的** - 编译期注册，分层匹配，零拷贝支持
- ✅ **类型安全的** - 编译期类型检查
- ✅ **线程安全的** - 完全并发支持，全局同步模式可选
- ✅ **灵活的** - 正则匹配，优先级控制，可扩展

Action 注册与分发系统，适用于事件驱动架构、消息路由、命令处理等场景。

---

**文档版本**: 1.0  
**创建日期**: 2024  
**最后更新**: 2024

